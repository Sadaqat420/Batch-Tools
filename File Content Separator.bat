@echo off
rem This batch file separates the content of a text file into multiple files based on a delimiter.
rem Instructions:
rem 1. Run the batch file.
rem 2. Enter the path of the text file you want to separate.
rem 3. Enter the delimiter by which you want to separate the content.
rem 4. The batch file will separate the content of the text file into multiple files based on the delimiter.
rem 5. Press any key to exit.

echo This batch file separates the content of a text file into multiple files based on a delimiter.
echo.
set /p file=Enter the path of the
